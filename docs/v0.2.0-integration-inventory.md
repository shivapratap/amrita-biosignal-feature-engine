# ABFE v0.2.0 integration inventory

This inventory records Release-Readiness Batch A for the
`codex/complexity-design` branch. It compares the published v0.1.0 source
commit `dcb6323e9dadbf54e98fd1de5aaaea696e6e253f` with the reviewed design commit
`8ce5602f59525ea31d45da1c1fa24a599fcfac93`.

The annotated `v0.1.0` tag object has a different object identifier; it
dereferences to the published source commit above.

## Scope conclusion

The comparison contains 24 changed tracked files: 3,069 inserted lines and 32
deleted lines before this inventory was added. Every changed file belongs to
the approved nine-feature fractal/complexity scope, its independent validation,
or v0.2.0 release preparation. No unrelated application feature, runtime
dependency, domain preset, filtering behavior, or PSD/entropy algorithm change
was found.

The candidate adds one scientific runtime module and keeps NumPy and SciPy as
the only runtime dependencies.

## Changed-file classification

| File | Classification | Disposition |
| --- | --- | --- |
| `.github/workflows/ci.yml` | CI/reference authority | Retain; adds the exact nolds validation pin |
| `CHANGES.md` | Release metadata | Retain; unreleased v0.2.0 scientific and audit record |
| `README.md` | User documentation | Retain; complexity catalogue and policy |
| `benchmarks/README.md` | Benchmark documentation | Retain; LZ76 methodology |
| `benchmarks/benchmark_baseline.py` | Benchmark implementation | Retain; deterministic LZ76 cases and current default count |
| `benchmarks/results/lz76-remediation.json` | Performance evidence | Retain; provenance strengthening assigned to Batch C |
| `docs/api-reference.md` | Public API documentation | Retain; complexity namespace and Lyapunov request |
| `docs/complexity-validation.md` | Normative scientific design | Retain; approved formulas and policies |
| `docs/v0.2.0-release-readiness.md` | Release design | Retain; staged integration and publication gates |
| `examples/api_smoke_test.py` | Executable documentation | Retain; direct and request-driven complexity examples |
| `pyproject.toml` | Version metadata | Retain; `0.2.0.dev0`, with no dependency change |
| `scripts/check_artifacts.py` | Artifact integrity | Retain; requires the complexity module |
| `scripts/smoke_installed.py` | Installed-artifact validation | Retain; exercises all nine features |
| `src/amrita_biosignal_feature_engine/__init__.py` | Public orchestration API | Retain; exports `LargestLyapunovRequest` |
| `src/amrita_biosignal_feature_engine/complexity.py` | Scientific implementation | Retain; all nine approved estimators |
| `src/amrita_biosignal_feature_engine/extractor.py` | Extraction orchestration | Retain; dispatch, request model, and provenance |
| `src/amrita_biosignal_feature_engine/feature_registry.py` | Registry metadata | Retain; eight defaults and one request-only entry |
| `tests/reference/test_complexity_reference.py` | Pinned external validation | Retain |
| `tests/test_complexity.py` | Independent/unit validation | Retain |
| `tests/test_extractor.py` | Integration validation | Retain |
| `tests/test_extractor_models.py` | Immutable model/provenance validation | Retain |
| `tests/test_feature_registry.py` | Registry contract validation | Retain |
| `tests/test_lz76_exhaustive.py` | Exhaustive independent oracle | Retain |
| `tests/test_public_api.py` | Export-policy validation | Retain |

## Commit classification

The nine feature-branch commits before this inventory are logically scoped:

1. `69a0b84` — numerical design;
2. `43f4428` — initial four complexity features;
3. `9965259` — Lempel-Ziv complexity;
4. `94489f0` — strengthened LZ validation and Fisher information;
5. `51a3982` — Higuchi fractal dimension;
6. `325f792` — detrended fluctuation analysis;
7. `de44ea7` — largest Lyapunov exponent;
8. `e02f88e` — independent-audit remediation;
9. `8ce5602` — v0.2.0 release-readiness design.

No commit combines an external publication action, tag, or release artifact
with the scientific implementation.

## Registry and feature-scope check

The intended scope is present:

- eight scalar complexity features with canonical defaults;
- one request-only largest Lyapunov exponent;
- 35 registered scalar features in total;
- 34 entries in `DEFAULT_FEATURE_NAMES`;
- no inferred Lyapunov scientific parameters;
- no runtime dependency beyond NumPy and SciPy.

## Pinned validation authorities

The clean Python 3.12 reference-validation job installs:

| Authority | Exact pin |
| --- | --- |
| AntroPy | `0.2.2` |
| MNE | `1.12.1` |
| nolds | `0.6.2` |
| sampen-profile | commit `263b72f7b906208241291c0579e8ec103fb52d79` |
| DIHC Feature Manager | commit `852a79d0178eb762aeaf81960820043365a059c3` |

These remain CI-only validation dependencies.

## Tracked-file hygiene

The repository had 72 tracked files at the reviewed design commit. Read-only
checks found:

- no tracked `.mypy_cache`, `.pytest_cache`, `.ruff_cache`, or `__pycache__`;
- no tracked bytecode, wheel, source-distribution, build, virtual-environment,
  or coverage output;
- no tracked independent audit report;
- no tracked `AGENTS.md`;
- no private-key, GitHub-token, or AWS access-key signature in reachable
  tracked files;
- no whitespace errors in the complete v0.1.0-to-v0.2.0 branch diff.

Local caches and `.DS_Store` files exist but are ignored and are not candidate
content. Batch A adds an explicit `.DS_Store` rule rather than relying on a
machine-global Git exclusion.

## Untracked-file disposition

`AGENTS.md` is the only untracked, non-ignored file at the reviewed design
commit. Its sole content identifies it as imported Claude Cowork audit
instructions. It is not ABFE governance, source, documentation, or release
material.

Disposition:

- retain the local file untouched;
- ignore `/AGENTS.md` explicitly;
- reject `AGENTS.md` from built artifacts;
- do not stage, commit, publish, or delete it as part of release readiness.

This resolves its release status without destroying external workspace
material.

## Independent-audit disposition

The focused remediation audit returned `PASS WITH OBSERVATIONS`. No audit
report is committed. Its three non-blocking observations—benchmark commit/input
hashes, explicit Lyapunov sampling-frequency regression, and LZ-specific
memory evidence—remain assigned to Release-Readiness Batch C.

## Batch A exit decision

Batch A passes:

- the branch diff is completely classified;
- the nine-feature scope is intact;
- external authorities are exactly pinned;
- generated material and audit files are excluded;
- the only external untracked file has an explicit non-destructive
  disposition;
- no release-blocking repository-integrity finding remains.
